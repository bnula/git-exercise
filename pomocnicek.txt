commandy co potrebujes pouzit:

vytvorit si folder, vliezt do neho, tieto veci:
pwd
cd ..
cd "meno-foldra"
dir
mkdir "meno-foldra"
touch "meno-filu"
"meno-filu"


git:
git clone "link" (ten si vies skopirovat z github repa z toho krasneho zeleneho buttonu na ktorom je napisane "<> Code")
git checkout -b "meno-branche"
git checkout "meno-branche"
git add -A / git add . (robia to iste)
git status
git diff
git commit -m "comment"
git pull "meno-branche"
git push -u origin "meno-branche"
git log
